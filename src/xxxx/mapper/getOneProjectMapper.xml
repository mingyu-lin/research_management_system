<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xxxx.mapper.getOneProjectMapper"><!--命名空间-->

    <select id="getOneProject" parameterType="int" resultType="xxxx.entity.Project">
        select * from project where project.projectid=#{id}
    </select>

</mapper>